#pragma once

#include <QWidget>

namespace FreeFit
{
    namespace GUI
    {
        class HashtagBar : public QWidget
        {
            public:
                HashtagBar(QWidget parent = nullptr) : QWidget(parent){}
        };
    }
}